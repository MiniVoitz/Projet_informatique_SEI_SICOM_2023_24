#include <stdlib.h>
#include <stdio.h>
#include "pyas/all.h"


int main() {
    // printf("Test 1 : Test des crochets\n");
    // char* regexp_char calloc(128,sizeof(char));
    // regexp_char = "[a-d]";
    // printf("L'expression régulière est %s\n", regexp_char);
    // printf("On s'attend à %s \n", "abcd One time");
    // list_t l = re_read(regexp_char);
    // reading(l);
    // printf("L'expression régulière sous forme de liste chaînée est \n");
    // printf("Présence de ^ : %d\n", (((binaryblock_t)(regexp_list->content)))->negation);
    // char* source = calloc(128,sizeof(char));
    //  = {'c', '7','\0'};
    // printf("La source sour forme de char est %s\n", source);
    // char *end=NULL;
    // int i;
    // i = re_match(regexp_list, source, &end);
    // printf("Après exécution de re_match, end vaut %p\n", end);
    // printf("i=%d\n",i);
    // printf("Le premier caractère de la suite de la source est %c\n", *end);
    return 0;
}
